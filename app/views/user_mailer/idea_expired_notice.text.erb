<!-- Hi <%= @idea.user.full_name ||= @idea.user.username %>,

We wanted to inform you that the campaign <%= @idea.title %> has met its expiration and is no longer active. <%= link_to "View your project", idea_url(@idea), target: "_blank" %>. -->
