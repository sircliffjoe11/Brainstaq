<div class="form-group col-sm-12">
  <h4>Post a Comment</h4>

<%= form_for [@idea, @comment] do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <p>
      <%= form.text_field :username, class: "form-control", placeholder: "Enter your Name" %>  
    </p>
  
    <%= form.text_area :body, id: :comment_body, class: "form-control", placeholder: "Enter Comment" %>
    <%= form.hidden_field :idea_id %>

    <div>
      <%= form.submit class: "actions btn btn1 text-white mt-3" %>
    </div>
    
  </div>
<% end %>